namespace DeckCard.Model
{
    public enum CardTypes
    {
        // clubs (♧), spades (♤), hearts (♥) and diamonds (♢)
        Diamonds = 3,
        Hearts = 2,
        Spades = 1,
        Clubs = 0,
    }
}
